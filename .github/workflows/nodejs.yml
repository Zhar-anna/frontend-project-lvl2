#Name of workflow
name: Node CI
on: push
uses: paambaati/codeclimate-action@v3.0.0
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: make lint
      - run: make test